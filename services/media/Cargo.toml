[package]
name = "authorworks-media-service"
version = "0.1.0"
edition = "2021"
description = "Media transformation (audio, video, graphics)"

[dependencies]
spin-sdk = "2.2.0"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["cdylib"]
