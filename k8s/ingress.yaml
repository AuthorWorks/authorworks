apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authorworks-ingress
  namespace: authorworks
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - authorworks.leopaska.xyz
        - api.authorworks.leopaska.xyz
      secretName: authorworks-tls
  rules:
    - host: authorworks.leopaska.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authorworks-platform
                port:
                  number: 80
    - host: api.authorworks.leopaska.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authorworks-platform
                port:
                  number: 80
---
# Tenant-specific ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authorworks-tenant-1-ingress
  namespace: authorworks-tenant-1
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - tenant1.authorworks.leopaska.xyz
      secretName: tenant-1-tls
  rules:
    - host: tenant1.authorworks.leopaska.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authorworks-tenant-1
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authorworks-tenant-2-ingress
  namespace: authorworks-tenant-2
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - tenant2.authorworks.leopaska.xyz
      secretName: tenant-2-tls
  rules:
    - host: tenant2.authorworks.leopaska.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authorworks-tenant-2
                port:
                  number: 80