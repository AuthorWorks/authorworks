apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - project.yaml
  - applicationset.yaml
  # Optional: Individual component apps for granular control
  # - components/logto.yaml
  # - components/workers.yaml
